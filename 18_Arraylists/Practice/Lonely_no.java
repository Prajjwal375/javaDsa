// Lonely Numbers in ArrayList

import java.util.*;
public class Lonely_no {
    public static lonely(ArrayList<Integer> list) {
        int n = list.size();
        for(int i=0; i<n; i++) {
            
        }
    }
    public static main void(String args[]) {
        ArrayList<Integer> List  = new ArrayList<>();
        List.addAll(Arrays.asList(10,6,5,8));
    }
}